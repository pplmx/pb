# Protocol Buffer

## instruction

```bash
# init
make init

# if you are a user, run this command to generate pb code
make gen

# if you are a developer, run this command to generate pb code
# Compare with the gen command, this command will run buf mod update, buf format, buf lint before generating pb code
make pb
```
